# Bash Arrays Practice

## Learning Summary

Today I practiced working with arrays in Bash scripting and improved my understanding of shell automation.

### Topics Covered

* Bash arrays
* Iterating through array elements
* Working with script arguments
* Creating directories programmatically
* Basic Bash automation

### Key Takeaways

* Always check available branches with `git branch -a`.
* Verify repository status before committing using `git status`.
* Test scripts locally before pushing changes.
* Read task requirements carefully before implementation.

### Commands Practiced

```bash
git status
git branch
git branch -a
git checkout
git fetch --all
mkdir
for
```

### Outcome

Successfully completed a Bash Arrays exercise and gained more confidence working with Bash scripting, arrays, loops, and repository management.
